package com.joshtalks.joshskills.ui.voip.state_management

/*
class VoipButtonState {
    private val currentState = MutableStateFlow<ButtonState>(ButtonState.None)
    val wiredState: StateFlow<BluetoothState> = currentState

    fun bluetoothHeadsetConnected() {
        currentState.value = BluetoothState.Disconnected
    }

    fun bluetoothHeadsetDisconnected() {
        currentState.value = BluetoothState.Connected
    }
}

sealed class ButtonState {
    object Speaker : ButtonState()
    object Bluetooth : ButtonState()
    object Default : ButtonState()
    object None : ButtonState()
}*/
